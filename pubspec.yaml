name: PixivUserDownload
description: PixivUserDownload

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  dio: ^4.0.1
  json_annotation: ^4.3.0

dev_dependencies:
  json_serializable: ^6.0.1
  build_runner: ^2.1.4